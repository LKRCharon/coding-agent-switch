# codex-for-me (primary, recommended)
model_provider = "cfm"
model = "gpt-5.4"
review_model = "gpt-5.4"
model_reasoning_effort = "high"

[model_providers.cfm]
name = "cfm"
base_url = "https://api-vip.codex-for.me/v1"
wire_api = "responses"
requires_openai_auth = true
env_key = "CFM_API_KEY"

[features]
multi_agent = true
